<% if user_signed_in? %>
  <p class="text-left">
    <%= link_to '投稿フォーム', new_post_path %>
  </p>
<% else %>
  <p class="text-left">
    <%= link_to '投稿フォーム', new_post_path %>
  </p>
<% end %>

<!--search_form_for → ransackが提供するフォームヘルパー-->
<%= search_form_for @search do |f| %>
  <%#= f.label :address_or_writings_or_user_name_cont, "Keyword" %>
  <%= f.search_field :address_or_writings_or_user_name_cont, class: 'form-control', placeholder: '検索ワード' %>
  <%= f.submit '検索', class: 'btn btn-primary' %>
<% end %>
